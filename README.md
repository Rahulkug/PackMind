# PrefixGraph
PrefixGraph is a local-first context engine for AI coding agents.  It indexes a repository into an AST-aware graph, then builds small, explained, token-budgeted context packs for coding questions. The goal is simple: stop making agents rediscover the same repo structure by dumping files into every prompt.
